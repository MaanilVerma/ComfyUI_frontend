# Stacked PR demo

A throwaway stack to see how GitHub's native stacked pull requests render.
Safe to close and delete — nothing here ships.

## Slice 1 — the note
The bottom of the stack. This PR targets `main`.

## Slice 2 — how it stacks
This PR targets slice 1's branch, not main. Its diff shows only this section.
